<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">X-Wing Trade Center</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="<%= ((typeof activepage !== 'undefined') && (activepage === 'fooldal')) ? "nav-link active" : "nav-link" %>" 
                aria-current="page" href="/">Főoldal</a>
          </li>
          <li class="nav-item">
            <a class="<%= ((typeof activepage !== 'undefined') && (activepage === 'profil')) ? "nav-link active" : "nav-link" %>"
                 aria-current="page" href="/profil">Profil</a>
          </li>

          <li class="nav-item">
            <a class="<%= ((typeof activepage !== 'undefined') && (activepage === 'hirdeteseim')) ? "nav-link active" : "nav-link" %>" 
                aria-current="page" href="/hirdetes">Hirdetéseim</a>
          </li>
        </ul>
        <form><a class="btn btn-outline-success" href="/login.html" role="button">Bejelentkezés</a></form>
        <!--<button class="btn btn-outline-success" type="submit">-->
      </div>
    </div>
  </nav>